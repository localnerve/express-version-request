{
  "name": "express-version-request",
  "version": "2.0.0",
  "description": "versions an incoming request to Express based on header or URL",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "rimraf ./coverage && mkdirp ./coverage && node --test --experimental-test-coverage --test-reporter=spec --test-reporter-destination=stdout --test-reporter=lcov --test-reporter-destination=coverage/lcov.info"
  },
  "keywords": [
    "express",
    "api",
    "version",
    "versioned",
    "versioning",
    "routing",
    "router",
    "api versioning",
    "api version",
    "header"
  ],
  "author": "Liran Tal",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lirantal/express-version-request.git"
  },
  "bugs": {
    "url": "https://github.com/lirantal/express-version-request/issues"
  },
  "homepage": "https://github.com/lirantal/express-version-request#readme",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "globals": "17.4.0",
    "eslint": "^10.2.0",
    "rimraf": "^6.1.3",
    "mkdirp": "3.0.1"
  }
}
